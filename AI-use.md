# AI Use Disclosure

**Tool Used:** Gemini
**Purpose:** Syntactical reference, diagnostic assistance, and markdown formatting.

**Details of Use:**
1.  **Debugging:** Utilized to diagnose a fatal Node.js crash caused by a stray character (`q`) on line 5 of `server.js`.
2.  **Syntax & Architecture:** Used as a reference for structuring the Express `cors` middleware configuration to correctly allow `http://127.0.0.1:5500` and resolving preflight caching issues.
3.  **Formatting:** Assisted in compiling the final API contract tables and formatting the final markdown report.

All architectural decisions, including the semantic HTML structure, strict separation of client/server logic, and server-side validation rules, reflect our own understanding and implementation of the required HTTP standards.